{
  "name": "gene",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve public"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.1"
  }
}
